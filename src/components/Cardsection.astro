---
import content from "../Data/siteContent.json";
const { cardSection } = content;
---

<section class="py-24 px-4 overflow-hidden">
  <h1 class="font-bold text-4xl text-center mb-4">
    See what you can do with Wraft
  </h1>
  <p class="text-sm text-secondary-gray-600 text-center mb-8">
    Case studies from some of our amazing customers who are hiring faster.
  </p>
  <div class="overflow-x-auto pb-4 px-24 no-scrollbar">
    <div class="inline-flex space-x-6 min-w-max">
      {
        cardSection.map((card) => (
          <div class="bg-primary-green-800 rounded-lg p-6 w-80 h-96 flex-shrink-0 shadow-md">
            <div class="bg-green-200 h-auto mb-4 rounded overflow-hidden">
              {card.imageUrl ? (
                <img
                  src={card.imageUrl}
                  alt={card.title}
                  class="w-full h-full object-cover"
                />
              ) : (
                <div class="w-full h-full flex items-center justify-center">
                  <span>No Image</span>
                </div>
              )}
            </div>
            <h3 class="font-bold text-base mb-2 text-secondary-white-100">
              {card.title}
            </h3>
            <p class="text-secondary-white-100 text-sm">{card.description}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>

<!-- Second card section -->

<section class="text-center py-12">
  <p class="text-sm">More about wraft</p>
  <h1 class="text-4xl font-bold">Be on brand, every time</h1>
  <p class="text-sm text-gray-600">
    Learn how Wraft features are designed to help your team work more
    effectively.
  </p>

  <!-- Scrolling Cards Section -->
  <div class="mt-8 overflow-x-auto flex justify-center max-w-[1200px] mx-auto">
    <div class="flex space-x-4 px-4 w-full">
      <!-- Card 1 -->
      <div
        class="flex-shrink-0 w-[20rem] h-[25vh] p-6 bg-secondary-gray-700 shadow-lg rounded-lg text-start"
      >
        <h2 class="text-xl font-bold mb-2">Branded Pages</h2>
        <p class="text-sm text-gray-600">
          Bring your brand assets, Letterpads to your corporate fonts. No colour
          spared.
        </p>
      </div>

      <!-- Card 2 -->
      <div
        class="flex-shrink-0 w-[20rem] h-[25vh] p-6 bg-secondary-gray-700 shadow-lg rounded-lg text-start"
      >
        <h2 class="text-xl font-bold mb-2">Shared Templates</h2>
        <p class="text-sm text-gray-600">
          Maintain your tone with templates, render conditionally. Every
          department shines.
        </p>
      </div>

      <!-- Card 3 -->
      <div
        class="flex-shrink-0 w-[20rem] h-[25vh] p-6 bg-secondary-gray-700 shadow-lg rounded-lg text-start"
      >
        <h2 class="text-xl font-bold mb-2">Use Conditions</h2>
        <p class="text-sm text-gray-600">
          Not all documents are same. Use custom logic to change templates, or
          toggle sections to sign your best clients.
        </p>
      </div>

      <!-- Card 4 -->
      <div
        class="flex-shrink-0 w-[20rem] h-[25vh] p-6 bg-secondary-gray-700 shadow-lg rounded-lg text-start"
      >
        <h2 class="text-xl font-bold mb-2">Emails</h2>
        <p class="text-sm text-gray-600">
          Email as attachments. Integrate your favourite provider, use in built
          templates, or create custom.
        </p>
      </div>
    </div>
  </div>
</section>

<!-- Green Section at the Bottom -->
<section
  class="mt-8 bg-green-100 py-6 max-w-[1200px] text-center mx-auto bg-primary-green-800 h-[20vh]"
>
  <div class="pt-2">
    <h2 class="text-xl font-bold text-secondary-white-100">
      Documents for ambitious companies
    </h2>
    <p class="text-sm text-secondary-white-100 mb-4">
      Self-host or use our cloud platform (soon)
    </p>
    <div class="flex justify-center space-x-4">
      <button
        class="bg-primary-green-500 text-secondary-white-100 px-6 py-2 rounded-lg hover:bg-primary-green-900"
        >Sign up</button
      >
      <button
        class="border border-green-600 text-secondary-white-100 hover:bg-primary-green-900 px-6 py-2 rounded-lg"
        >Demo</button
      >
    </div>
  </div>
</section>
